{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",

  "//Integration": [
    "Link to the Logistics platform (logistics.desiconapp.com).",
    "Secrets are NOT stored here — set them in Azure App Service configuration:",
    "  Integration__InboundKey           the key Logistics must send us",
    "  Integration__Logistics__ApiKey    the key we send to Logistics",
    "Left blank the integration simply no-ops, so local and Docker runs are unaffected."
  ],
  "Integration": {
    "InboundKey": "",
    "Logistics": {
      "BaseUrl": "",
      "ApiKey": ""
    }
  }
}
